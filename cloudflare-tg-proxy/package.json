{
  "name": "tg-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.error('Dormant fallback: do not run this Worker without explicit approval'); process.exit(1)\"",
    "deploy": "node -e \"console.error('Dormant fallback: do not deploy this Worker without explicit approval'); process.exit(1)\""
  }
}
